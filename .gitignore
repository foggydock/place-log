# 注：js/config.js は publishable key（ブラウザに必ず出る＝公開して安全な鍵）+ URL のみ。
# デプロイに必要なのでコミットする。データは DB 側の RLS で守る。
# 秘密の secret / service key は絶対にここに入れないこと。

# OS / ローカル開発用
.DS_Store
*.swp
.claude/

# 書き出したバックアップは git に入れない
data/
backup/
*.backup.json
行った場所ノート_backup_*.json
行った場所ノート_backup_*.md
